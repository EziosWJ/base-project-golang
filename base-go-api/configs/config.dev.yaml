swagger:
  enabled: true

database:
  url: postgres://127.0.0.1:54329/base_go_api?sslmode=disable
  username: base_go_api
  password: replace-with-a-local-development-password

jwt:
  secret: replace-with-a-random-local-development-jwt-secret

log:
  level: debug
  format: text
