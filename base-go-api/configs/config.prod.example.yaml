# 生产环境配置模板。
# 实际 configs/config.prod.yaml 以只读文件或挂载方式由部署系统提供，不提交 Git。
# 所有占位符值必须在部署时替换为真实值。
swagger:
  enabled: false

database:
  url: postgres://db.example.internal:5432/base_go_api?sslmode=require
  username: change-me
  password: change-me

jwt:
  secret: change-me

log:
  level: info
  format: json
